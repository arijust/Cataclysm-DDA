{
  "$comment": "Declares the project-local overlay-ports path so vcpkg picks up our sdl3 override (>= 3.4.0) without bumping the registry baseline. Remove the overlay once the registry baseline catches up.",
  "default-registry": {
    "kind": "git",
    "repository": "https://github.com/microsoft/vcpkg",
    "baseline": "c3867e714dd3a51c272826eea77267876517ed99"
  },
  "overlay-ports": [
    "../vcpkg/overlay-ports"
  ]
}
